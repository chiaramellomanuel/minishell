#./ls esegue il comando ls mentre dovrebbe cercare un file
#nominato ls nella cartella corrente
./ls

#inserire + o - crasha il programma

#qualsiasi comando che porta ad un exit() di un processo figlio
#genera un leak per la variabile env
#la variabile non puo' essere liberata ad ogni errore in quanto serve per 
#il mantenimento dell'environment all'interno del ciclo di loop
#viene liberata all'uscita del programma
echo "ls" | wc -l
